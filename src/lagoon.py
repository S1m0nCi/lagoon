# Start with the basic elements of a programming language

from interpret import Interpret
from mathematics import Mathematics


class Lagoon():

  def __init__(self, code):
    self.code = code

  def execute(code):
    lag_exec = Interpret(code)
    print (lag_exec)